// Test framework kurulu olmadığı için şimdilik yorum satırında
/*
import productsReducer from '../productsSlice';

describe('productsSlice', () => {
  it('should return the initial state', () => {
    const initialState = productsReducer(undefined, { type: 'unknown' });
    
    expect(initialState).toEqual({
      ids: [],
      entities: {},
      status: 'idle',
    });
  });

  // Daha fazla test RTK Query matcher'ları için eklenebilir
  // Örnek: pending, fulfilled, rejected durumları için testler
});
*/

export {}; // TypeScript modül olarak tanıması için 